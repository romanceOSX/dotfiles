# .envrc
# 1. Sources your dynamic .env key layout instantly without touching Nix state
dotenv_if_exists .env

# 2. Automatically chains your local devShell if a flake.nix sits next to it
if [ -f flake.nix ]; then
  use flake . --impure
fi
