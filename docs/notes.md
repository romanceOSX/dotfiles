### Installables
Nix commands operate on installables, these are something that can be 
realized in the Nix store

### Flake output attribute
flakeref[#attr-path]

flakeref could be
- URL
- local path

attr-path default is 'packages.system.default', this depends on the command


### Store path


